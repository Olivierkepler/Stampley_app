/** Shown after request — same whether the email exists (privacy). */
export const PASSWORD_RESET_REQUEST_MESSAGE =
  "If an account exists for that email address, you will receive password reset instructions shortly.";
